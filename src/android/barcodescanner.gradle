repositories{
    jcenter()
    flatDir{
        dirs 'libs'
    }
}

dependencies {
implementation (files("barcodescanner-release-2.1.5.aar"))
//compile(name:'barcodescanner-release-2.1.5', ext:'aar')
}

android {
    packagingOptions {
        exclude 'META-INF/NOTICE'
        exclude 'META-INF/LICENSE'
    }
}
